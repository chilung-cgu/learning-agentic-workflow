## 1.0.19 - 2026-04-06

- /mcp enable 和 /mcp disable 現在會在跨工作階段之間保留
- OpenTelemetry 監控：子代理 span 現在使用 INTERNAL span 類型，而聊天 span 會包含 `github.copilot.time_to_first_chunk` 屬性（僅限串流）
- 缺少執行權限的外掛掛鉤腳本現在可在 macOS 上正常執行
- 當恢復的工作階段中代理顯示名稱與其檔名不同時，自訂代理現在會正確還原
- 當工作階段已被另一個用戶端使用時，會略過 IDE 自動連線
- 斜線命令的時間軸項目現在會包含命令名稱（例如「Review」、「Plan」），以提供更好的上下文

---

## 1.0.18 - 2026-04-04
- 新的 Critic 代理使用補充模型自動審查計劃和複雜的實現，以儘早發現錯誤（在 Claude 模型的實驗模式下可用）
- 會話恢復選擇器在首次使用時會按分支和儲存庫正確對會話進行分組
- preToolUse鉤子permissionDecision「允許」現在抑制工具批准提示
- 新增在 shell 完成、權限提示、誘導對話方塊和代理完成時非同步觸發的通知掛鉤事件

## 1.0.17 - 2026-04-03
- CLI 中現已包含內建技能，首先是自訂 Copilot 雲端代理環境的指南
- MCP OAuth 流現在透過自簽名憑證回退支援 HTTPS 重定向 URI，從而提高了與需要 HTTPS 的 OAuth 提供者（例如 Slack）的兼容性
- /resume 會話選擇器載入速度明顯加快，尤其是對於大型會話歷史記錄

## 1.0.16 - 2026-04-02
- 當透過 excludedTools 或 availableTools 排除 sql 工具時，SQL 提示標籤不再出現
- MCP工具呼叫在時間軸中顯示工具名稱和參數摘要
- 當工作目錄變更時，MCP 伺服器透過有效身份驗證正確重新連接
- 新增 PermissionRequest 掛鉤以允許腳本以程式設計方式批准或拒絕工具權限請求
- 刪除已棄用的 `marketplaces` 儲存庫設定（使用 `extraKnownMarketplaces` 取代）
- MCP 伺服器在登入、使用者切換和 /mcp 重新載入後正確載入
- BYOK Anthropic 提供者現在遵循配置的 maxOutputTokens 限制
- 刪除已棄用的 `marketplaces` 儲存庫設定（使用 `extraKnownMarketplaces` 取代）

## 1.0.15 - 2026-04-01
- 刪除對 gpt-5.1-codex、gpt-5.1-codex-mini 和 gpt-5.1-codex-max 型號的支持
- 副駕駛吉祥物現在在互動模式下以微妙的眼睛動畫閃爍
- 使用者切換器和 `/user list` 依字母順序顯示帳戶
- 新增 mcp.config.list、mcp.config.add、mcp.config.update 和 mcp.config.remove 伺服器 RPC，用於管理持久性 MCP 伺服器配置
- 新增設備代碼流 (RFC 8628) 作為無頭和 CI 環境中 MCP OAuth 的後備
- 為具有帳戶切換支援的 MCP OAuth 伺服器新增 `/mcp auth` 命令和重新驗證 UI
- 為工具錯誤新增 postToolUseFailure 掛鉤，並使 postToolUse 僅在成功的工具呼叫後執行
- 新增 /share html 指令將會議和研究報告匯出為獨立的互動式 HTML 文件
- 按 Escape 或 Ctrl+C 取消後，自動駕駛不再繼續
- CLI 載入時鍵入的按鍵不再遺失
- 大型工具輸出預覽顯示正確的字元數和最多 500 個字符
- 將 Home/End 和 Page Up/Page Down 導覽新增至差異檢視器
- CLI 在會話結束後立即退出，而不是等待 10 秒
- 配置設定askUser、autoUpdate、storeTokenPlaintext、logLevel、skillDirectories和disabledSkills現在使用camelCase名稱（snake_case仍然接受）
- 許多設定鍵現在更喜歡駝峰命名（蛇形命名仍然有效）
- Ctrl+D 不再排隊訊息；使用 Ctrl+Q 或 Ctrl+Enter 排隊
- 連接速度慢的 MCP 伺服器不再阻止代理啟動
- 現在可以在 WSL 環境中貼上 Windows 剪貼簿中的映像

## 1.0.14 - 2026-03-31
- 使用 BYOM 時，圖像會正確傳送到 Anthropic 模型
- 模型選擇器選擇正確覆蓋目前會話的 --model 標誌
- 終端輸出不再在錯誤退出時清除或跳轉
- Shift+Enter 在支援 Kitty 鍵盤協定的終端機中插入換行符
- 當 Git 市場 URL 克隆失敗時顯示底層錯誤詳細信息
- 臨時檔案操作不再在 macOS 上觸發不必要的權限提示
- 允許 SDK 會話參與者透過 handlePendingElicitation API 回應啟發請求
- 會話結束時會正確清理 Shell 進程
- SDK exit_plan_mode.requested 事件現在總是發出，無論是否配置了直接回調
- 使用 Microsoft Entra ID 驗證的 MCP 伺服器不再在每次登入時顯示同意畫面
- 當達到逾時時，Grep 和 glob 搜尋結果會立即傳回
- 在引出對話方塊中快速輸入時，按鍵不再遺失
- 本機 Windows 上的剪貼簿副本不再在貼上文字的開頭包含雜散的 U+FEFF 字符
- 修正了恢復會話時忽略 --config-dir 的問題，導致路徑默默回退到 ~/.copilot
- 被白名單原則阻止的 MCP 伺服器現在在 /mcp show 中隱藏
- 使用自帶模型 (BYOM) 提供者時，推理工作設定現在可以正確應用
- 使用經典 PAT 時確保清晰的錯誤訊息
- grep 工具處理大檔案和長行而不會耗盡內存
- 當 CLI 在 ACP 模式下運作時，MCP 伺服器 OAuth 驗證有效
- 在空格上拆分 $BROWSER
- 當滑鼠支援處於活動狀態時，貼上的文字不再損壞
- 卸載市場插件會從磁碟中刪除其快取數據
- 透過最佳化旋轉器渲染和任務輪詢來減少串流期間的 CPU 使用率
- 透過並行運行終端檢測、身份驗證和 git 操作來減少 CLI 啟動時間
- 透過自動重試和請求逾時，MCP 註冊表查找更加可靠
- 由於 V8 編譯快取減少了重複呼叫的解析和編譯時間，CLI 啟動速度更快
- 刪除gemini-3-pro-preview模型的支持

## 1.0.13 - 2026-03-30
- 會話結束時會正確清理 Shell 進程
- 透過最佳化旋轉器渲染和任務輪詢來減少串流期間的 CPU 使用率
- SDK exit_plan_mode.requested 事件現在總是發出，無論是否配置了直接回調
- 使用 Microsoft Entra ID 驗證的 MCP 伺服器不再在每次登入時顯示同意畫面
- 當達到逾時時，Grep 和 glob 搜尋結果會立即傳回
- 在引出對話方塊中快速輸入時，按鍵不再遺失
- 本機 Windows 上的剪貼簿副本不再在貼上文字的開頭包含雜散的 U+FEFF 字符
- 修正了恢復會話時忽略 --config-dir 的問題，導致路徑默默回退到 ~/.copilot
- 透過並行運行終端檢測、身份驗證和 git 操作來減少 CLI 啟動時間
- /rewind 和雙 Esc 現在打開一個時間軸選擇器，可以回滾到對話歷史記錄中的任何點，而不僅僅是上一個快照
- 透過自動重試和請求逾時，MCP 註冊表查找更加可靠
- 由於 V8 編譯快取減少了重複呼叫的解析和編譯時間，CLI 啟動速度更快
- MCP 伺服器可以透過新的審核提示請求 LLM 推理（採樣）並獲得使用者批准
- 被白名單原則阻止的 MCP 伺服器現在在 /mcp show 中隱藏
- 使用自帶模型 (BYOM) 提供者時，推理工作設定現在可以正確應用
- 使用經典 PAT 時確保清晰的錯誤訊息
- grep 工具處理大檔案和長行而不會耗盡內存
- 當 CLI 在 ACP 模式下運作時，MCP 伺服器 OAuth 驗證有效
- 在空格上拆分 $BROWSER
- 當滑鼠支援處於活動狀態時，貼上的文字不再損壞
- 卸載市場插件會從磁碟中刪除其快取數據
- 刪除gemini-3-pro-preview模型的支持

## 1.0.12 - 2026-03-26
- 當工作目錄是 git root 時，.mcp.json 中定義的 MCP 伺服器可以正確啟動
- 當非系統 Clip.exe 遮蔽 PATH 中的系統時，剪貼簿複製可以在 Windows 上正常運作
- 當行內反白時，/diff 視圖正確渲染所有行
- 插件掛鉤現在接收 CLAUDE_PROJECT_DIR 和 CLAUDE_PLUGIN_DATA 環境變量，並支援掛鉤配置中的 {{project_dir}} 和 {{plugin_data_dir}} 模板變量
- 工作區 MCP 伺服器現在已正確載入並且對代理可見
- /clear 在新會話中保留 MCP 伺服器
- 模型顯示標題在模型名稱旁顯示主動推理工作水準（例如「（高）」）
- /session rename 在沒有名稱參數的情況下呼叫時會從對話記錄中自動產生會話名稱
- 刪除 --alt-screen 標誌和 alt_screen 設定；替代畫面現在始終啟用
- 現在可以在 VS Code 終端中點擊 OSC 8 超鏈接
- PowerShell /flag 參數（例如 /all、/enum-devices）不再被錯誤地視為檔案路徑
- 受信任的資料夾存取提示不再在 Windows OneDrive 路徑和不區分大小寫的檔案系統上錯誤顯示
- 狀態列負載包括 session_name 欄位和 session_id
- @檔案選擇器不再顯示.git目錄內容
- 調整終端大小時，滾動位置不變
- 使用 /clear 啟動新會話後 /yolo 路徑權限仍然存在
- 在終端文字選擇中正確選擇並突出顯示表情符號字符
- 具有活躍工作的會話不再被過時的會話收割者清理
- 恢復會話恢復之前選擇的自訂代理
- 當執行產生大量輸出的 shell 命令時，CLI 不再因記憶體不足錯誤而崩潰
- 在自動駕駛取消期間多次按 Escape 不會再導致會話卡住
- 讀取 .claude/settings.json 和 .claude/settings.local.json 作為附加儲存庫配置來源
- 模型選擇器以全螢幕視圖打開，並使用 ← / → 箭頭鍵調整內嵌推理工作量
- OTEL 掛鉤執行現在記錄為跨度事件而不是子跨度，從而減少了追蹤混亂
- 按 Enter 後立即在對話中出現使用者提示
- /allow-all (/yolo) 現在支援 on、off 和 show 子命令來啟用、停用或檢查允許所有模式
- 在計劃模式下Ctrl+Y 在尚無計劃時開啟最新的研究報告

## 1.0.11 - 2026-03-23
- 確保模型正確顯示在選擇器中，盡可能顯示模型名稱
- 當 MCP 伺服器被政策阻止時顯示警告（例如執行白名單）
- 現在對所有使用者強制執行第三方 MCP 伺服器的組織策略
- 新增 ~/.agents/skills/ 作為個人技能發現目錄，與 VS Code 的 GHCP4A 擴展預設值保持一致
- 來自多個擴展的擴展掛鉤現在合併，而不是相互覆蓋或覆蓋 hooks.json 中的掛鉤
- sessionStart鉤子additionalContext現在被注入到對話中
- /clear 現在完全放棄當前會話，而 /new 開始新的對話（保持舊會話處於背景）
- 連接到遠端主機時尊重 GitHub MCP 伺服器使用者配置
- 進程掛起和恢復後終端螢幕正確重繪（Ctrl+Z / fg）
- MCP OAuth 驗證可與 Atlassian Rovo MCP 伺服器等 MCP 伺服器搭配使用，該伺服器支援動態用戶端註冊，但在非標準 URL 上託管授權元數據
- /cd 每個會話保留一個單獨的工作目錄，切換會話時恢復
- 現在可以在從工作目錄到 git 根目錄的每個目錄層級發現自訂指令、MCP 伺服器、技能和代理，從而實現完整的 monorepo 支持
- 啟動「環境已載入」訊息現在顯示已載入掛鉤的數量
- 後台代理進度（目前意圖和工具呼叫已完成）現在顯示在 read_agent 和任務逾時回應中
- statusLine.command 路徑現在支援 ~ 和環境變數（例如 $HOME、${VAR:-default}）
- /new 和 /clear 命令接受可選提示以使用第一則訊息啟動新會話

## 1.0.10 - 2026-03-20
- 減少查看完整大檔案時的記憶體使用量
- /login 設備流程在程式碼空間和遠端終端環境中正常運作
- 在遠端會話中使用 --server 模式時，可以正確偵測工作目錄
- 箭頭鍵在使用應用程式鍵盤模式的終端機中正常運作
- 使用提示模式（-p 標誌）時，回購掛鉤 (.github/hooks/) 現在可以正確觸發
- /copy 將格式化的 HTML 寫入 Windows 上的剪貼簿，以便貼到 Word、Outlook 和 Teams 中
- SDK 用戶端可以在啟動或加入會話時註冊自訂斜線命令
- SDK 用戶端可以透過 session.ui.eliitation 向使用者顯示誘導對話框
- 添加對多個並發會話的實驗性支持
- 新增 --effort 作為 --reasoning-effort 的簡寫別名
- 新增/undo指令以撤銷上一回合並恢復檔案更改
- 當內容包含硬換行符號時，Markdown 項目符號清單可以在替代畫面模式下正確呈現
- 引出表單顯示 Shift+Tab 提示，用於在欄位之間反向導航
- 遠端會話 URL 顯示為緊湊的可點擊「在瀏覽器中開啟」鏈接，而不是重複的原始 URL
- 透過 /quit、Ctrl+C 或重新啟動退出時，會話歷史記錄不再遺失
- 嵌套鉤子結構中定義的鉤子匹配器過濾器現在可以正確應用於內部鉤子項目
- 使用 .claude-plugin/ 或 .plugin/ 清單目錄的插件現在可以正確載入其 MCP 和 LSP 伺服器
- /terminal-setup 不再向 WSL 使用者顯示誤導性錯誤
- 模型選擇器根據使用者計劃和策略將模型重新組織為可用、封鎖/停用和升級選項卡
- 來自 .mcp.json、.vscode/mcp.json 和 devcontainer.json 的工作區 MCP 伺服器現在僅在確認資料夾信任後才加載
- 配置設定重新命名為駝峰命名法：`includeCoAuthoredBy`、`effortLevel`、`autoUpdatesChannel`、`statusLine`（舊名稱仍然有效）
- 複製助理回應時，前導 2 空格 UI 縮排將從所有選取行共用該縮排的選擇中刪除
- 透過 --plugin-dir 載入的插件現在顯示在單獨的「外部插件」部分下的 /plugin 清單中

## 1.0.9 - 2026-03-19
- 在 SSH 斷開連接或終端關閉期間，虛假 I/O 錯誤訊息（ENOTCONN、EIO）不再出現在時間軸中
- 新增 include_gitignored 設定選項以在 @檔案搜尋中包含 gitignored 檔案
- 在 WSL 上複製文字可以正確保留 CJK 和其他非 ASCII 字符
- 從縮短的 URL（例如 aka.ms 連結）安裝的市場和插件現在可以正常工作

## 1.0.8 - 2026-03-18
- 代理模式標籤和邊框在非真彩色終端（tmux、SSH、螢幕）上顯示正確的顏色
- 現在預設啟用備用螢幕緩衝區，以獲得更清晰的終端體驗
- 當擴展子流程加入活動會話時，退出計劃模式工具仍然可用
- 僅在確認資料夾信任後載入儲存庫層級掛鉤，而不是在顯示信任對話框之前載入
- 空閒子代理不再使 /tasks 視圖混亂 - 它們在 2 分鐘不活動後隱藏
- 新增擴展模式設定以控制擴展性
- 可使用實驗性 MCP_ALLOWLIST 功能標誌根據已設定的登錄機碼驗證 MCP 伺服器
- 允許 --resume 除了會話 ID 之外也接受任務 ID
- 支援在settings.json、settings.local.json和config.json中定義hooks
- 滾動在 macOS Terminal.app 和其他不支援 SGR 滑鼠編碼的終端中正常工作
- 從外部編輯器返回後，滑鼠滾動在 tmux 中可以正常工作
- 提示模式下的 Ctrl-C 現在立即退出，而不是等待請求完成
- 旋轉動畫不再延遲可見輸出出現在時間軸中
- 對話方塊標題在所有對話方塊內一致顯示

## 1.0.7 - 2026-03-17
- 改善 CLI 主題之間的色彩對比度，以提高可讀性和可訪問性
- 用戶訊息以微妙的背景顏色顯示，以便在視覺上與助理訊息區分開來
- 增加對 gpt-5.4-mini 型號的支持
- 選項卡欄所選選項卡使用緊湊的[標籤]樣式，間距更清晰
- 將「自訂」模式新增至系統訊息配置中，以覆寫部分層級的系統提示
- 當文字存在時，雙擊 Esc 清除輸入，或當提示為空時觸發撤消，並在第一個 Esc 後顯示提示
- 對於 1.0.6 之前建立的會話，會話恢復不再失敗並顯示“會話檔案已損壞”
- 分支指示器區分標頭中的未暫存變更 (\*)、暫存變更 (+) 和未追蹤檔案 (%)
- 新增實驗性 SDK 會話 API 來列出和管理技能、MCP 伺服器和插件，並具有從工作目錄自動發現可選配置的功能
- 新增 subagentStart 鉤子，該鉤子在產生子代理時觸發，支援將附加上下文注入子代理的提示中
- 專業版和試用版用戶現在可以在模型選擇器中看到他們有權使用的所有模型
- CLI 重新啟動不再將 -i/--interactive 提示重新傳送到新會話
- 解決自動更新可能在 Windows 上留下不完整套件的邊緣情況

## 1.0.6 - 2026-03-16
- 在上一回合出錯後，自動駕駛儀的繼續運作不再被永久阻止
- 在自動駕駛儀中，現在需要task_complete摘要並呈現為markdown
- 每次提示提交時，螢幕閱讀器不再大聲朗讀輸入佔位符文本
- shell指令解析後釋放tree-sitter WASM物件以防止記憶體洩漏
- /help 對話方塊開始在替代螢幕模式下捲動到頂部
- 自動更新可以從 Windows 上的競爭條件中正確恢復
- 更新後，當另一個實例正在運行時，CLI 不再無法在 Windows 上載入
- 透過消除每個子進程產生的冗餘環境變數副本來減少記憶體使用
- 剩餘請求小工具不再為 Copilot 免費使用者顯示不準確的配額數據
- 解決子代理處於活動狀態時由 HTTP/2 連線池競爭條件所造成的會話崩潰
- CLI 在自動更新後載入自身的最新版本
- Kill 命令驗證不再錯誤地阻止某些合法命令。例如python 腳本中的 p.kill()
- 指令檔 frontmatter applyTo 欄位接受字串和陣列值
- 改善流和工具輸出記憶體使用
- 模型可以透過克勞德模型的工具搜尋動態發現和使用工具
- 恢復上一個會話時鉤子正確觸發
- 在替代螢幕模式下提示輸入會呈現所有行而不會截斷
- 在 VS Code 的整合終端中運行時，連結和右鍵貼上不再觸發兩次
- Hook 設定檔現在可以在 VS Code、Claude Code 和 CLI 上工作，無需修改，接受 PascalCase 事件名稱和駝峰命名法
- 本機模組預先建置（例如 Windows ARM64 上的 conpty.node）在首次啟動時可靠地載入
- /tasks 視圖中的子代理運行時間在空閒時凍結，並在再次活動時恢復
- 標誌 --enable-all-github-mcp-tools、--add-github-mcp-toolset 和 --add-github-mcp-tool 現在在使用 SDK 時生效（ACP 模式）
- 使用 COPILOT_CUSTOM_INSTRUCTIONS_DIRS 時可以正確載入自訂指令檔案路徑
- 當指令導致 shell 退出時，指令輸出不再遺失
- 透過 --plugin-dir 載入時會發現使用 .claude-plugin/plugin.json 的插件
- 修正了使用舊的 /terminal-setup 配置在 VS Code 上處理 shift+enter 的問題。
- 代理建立精靈顯示正確的使用者代理目錄路徑
- 支援開放插件規格文件位置以載入插件和市場清單
- 顯示更友善的錯誤訊息並提供鍵盤快捷鍵以在瀏覽器中開啟事件鏈接
- 擴充工具現在可以與權限系統搭配使用，每個工具使用 `skipPermission` 來繞過權限提示
- Hook 設定檔現在支援 Claude Code 的嵌套匹配器/hooks 結構和可選類型字段
- 任務工具啟動的子代理根據其名稱分配人類可讀的 ID（例如 `math-helper-0`），而不是通用 `agent-0` 標識符
- create_pull_request 工具現在在其輸出中包含 PR URL，以便代理可以共享直接鏈接
- read_agent 輸出包括觸發多輪代理中每一輪的入站訊息
- 提高與開放外掛程式規格的相容性：支援 `.lsp.json`、PascalCase 掛鉤事件名稱、`exclusive` 路徑模式與 `:` 命名空間分隔符

## 1.0.5 - 2026-03-13
- 執行 /clear 或 /new 後終端標題重設為預設值
- 新增 /extensions 指令來檢視、啟用和停用 CLI 擴充
- @ 檔案提及現在支援專案外部的路徑：絕對路徑 (@/usr/...)、主目錄 (@~/...) 和相對父路徑 (@../...)
- 使用 /experimental on|off 切換實驗模式會自動重新啟動 CLI 以立即套用變更
- 右鍵點選貼上前往活動對話方塊輸入而不是主對話輸入
- 引入 /pr 來幫助建立和查看 PR、自動修復 CI 故障、處理審核回饋並解決合併衝突
- 封鎖網路 (UNC) 路徑以防止透過 SMB 驗證洩漏憑證
- 使用 write_agent 工具向背景代理發送後續訊息以進行多輪對話
- 記憶體儲存錯誤現在會指示儲存庫不存在或您缺乏寫入存取權限
- 在環境變數中設定經典個人存取權杖 (ghp\_) 時顯示明顯的錯誤，而不是靜默退出
- 差異視圖在 Windows 上正確顯示，而不是顯示損壞/覆蓋的文本
- 修復關閉時出現的 Kitty 鍵盤協定轉義序列
- 現在可以正確保留將 claude-sonnet-4.6 設定為預設模型
- 插件卸載使用儲存的安裝路徑可靠地刪除文件
- 新增 /version 指令以顯示 CLI 版本並檢查會話內的更新
- 新增實驗性基於嵌入的動態檢索 MCP 和每回合技能指令
- /diff 中的語法高亮顯示，支援 17 種程式語言
- 新增 preCompact 掛鉤以在上下文壓縮開始之前執行命令
- 當重試次數用盡後發生錯誤時，來自 API 的請求 ID 現在會顯示在時間軸中
- 帶有反引號格式代碼的 PR 描述在 Windows/PowerShell 上正確呈現
- 當檔案路徑作為 CLI 命令傳遞時顯示有用的錯誤訊息
- 當令牌無效或過期時，會話報告身份驗證錯誤而不是掛起
- 檢視工具顯示大型單行檔案（例如縮小的 JS、大型 JSON blob）的部分內容，而不是空輸出
- /changelog 支援 `last <N>`、`since <version>` 和 `summarize` 一次瀏覽和總結多個發行說明
- CLI 現在接受省略版本欄位的 Hooks 設定文件

## 1.0.4 - 2026-03-11
- 新增 `session.shell.exec` 和 `session.shell.kill` RPC 方法，用於透過串流 stdout/stderr 輸出執行 shell 指令
- 來自 --plugin-dir 插件的自訂代理現在可以在 ACP 模式下正確載入
- 具有動態色彩模式和互動式主題選擇器的自適應色彩引擎。在有限顏色終端和 Windows 上正常降級
- 當回呼連接埠變更或使用 Microsoft Entra ID 時，MCP OAuth 重新驗證可以可靠地工作
- 將 /pr open 替換為 /pr view [local|web] 以在本機檢視 PR 狀態或在瀏覽器中開啟
- 啟用 OpenTelemetry 工具，以便觀察代理會話、LLM 呼叫和工具執行
- 擴充功能現在可以編寫為 CommonJS 模組 (extension.cjs)
- 在環境載入啟動訊息中顯示載入的擴充計數
- 支援disableAllHooks標誌以停用設定檔中的所有掛鉤
- 支援會話日誌中的 Azure DevOps 儲存庫識別
- 會話匯出標頭在共用要點中將每個欄位呈現在自己的行上
- 現在，在 SAML 執行錯誤時，無需身份驗證令牌即可重試自動更新
- 自動駕駛模式在 API 錯誤後停止繼續，而不是無限循環
- 透過使用最後一次呼叫的輸入和輸出標記而不是累積總數，狀態行上下文視窗百分比不再在回合中膨脹
- 使用備用畫面時暫停時正確停用 Kitty 鍵盤協議
- 僅當推理標題是唯一可用的推理文字時才顯示推理標題。
- CLI 崩潰時終端會正確重置，防止 shell 損壞
- /update 命令自動重新啟動以套用更新，而不需要手動退出
- OAuth 驗證現在可以透過適當的資源指示器和刷新令牌支援可靠地處理 Microsoft Entra ID 和其他 OIDC 伺服器
- 在 /instructions 選擇器中顯示單一指令檔名，並帶有註入檔案的 [external] 標籤
- 除了將路徑新增至允許清單之外，路徑權限對話方塊還提供一次性批准選項
- 新增 --reasoning-effort CLI 標誌來設定推理工作級別
- Hooks 現在可以在工具執行之前透過「詢問」權限決定請求使用者確認
- 新增配置副駕駛子代理，用於透過任務工具管理 MCP 伺服器、自訂代理和技能
- 互動式 shell 初始化在慢速機器上不再超時
- 透過跳過 PowerShell 設定檔加載，在 Windows 上運行更快的 shell 命令
- 改進 CLI 幫助文件以使用標準 --option=value 格式和逗號分隔清單語法

## 1.0.3 - 2026-03-09
- 預設為員工使用者啟用備用螢幕緩衝區
- 擴展現在作為實驗性功能提供 - 要求 Copilot 使用 @github/copilot-sdk 為自己編寫自訂工具和掛鉤
- 在說明中記錄 GH_HOST、HTTP_PROXY、HTTPS_PROXY、NO_COLOR 和 NO_PROXY 環境變量
- 從 .devcontainer/devcontainer.json 讀取 MCP 伺服器配置
- 新增 --binary-version 標誌以查詢 CLI 二進位版本而無需啟動
- 新增 /restart 指令以熱重啟 CLI，同時保留會話
- 後台任務通知顯示在時間軸中，並具有可擴展的詳細信息
- 除了「exit」之外，也鍵入「quit」退出 CLI
- 新增 extraKnownMarketplaces 儲存庫設定以取代市場
- 將 Windows 終端支援新增至 /terminal-setup 命令
- /reset-allowed-tools 現在完全撤銷 /allow-all 並重新觸發自動駕駛權限對話框
- 改進了 SQL 工具中批次查詢的處理
- 當系統金鑰環無回應時，登入流程不再在 Ubuntu 上掛起
- 當 CLI 意外崩潰時，終端會正確重置
- 表格在螢幕閱讀器模式下停用邊框，以防止顯示裝飾字符
- 現在可以存取具有不合格輸出模式的 MCP 伺服器
- /plugin 更新現在適用於 GitHub 安裝的插件
- /add-dir 目錄在會話變更期間持續存在，例如 /clear 和 /resume
- 防止 env 命令被視為安全而未經批准而允許
- 在狹窄的終端機中換行時，佔位文字顏色可以正確顯示
- /plugin update 現在適用於專案設定中定義的市場
- 現在顯示重試狀態訊息以顯示伺服器錯誤復原期間的進度
- 在取得變更時以差異模式顯示載入微調器
- 當 .github/instructions/ 包含指令時抑制 /init 建議
- 將 merge_strategy 配置重新命名為 mergeStrategy 以保持一致性
- 抑制技能和命令前沿中的未知字段警告
- 信任安全的 sed 指令無需確認即可運行

## 1.0.2 - 2026-03-06
為了紀念 GitHub Copilot CLI 上週全面可用，我們將主要版本增加到 1.0！

- 輸入「exit」作為裸指令來關閉 CLI
- Ask_user 表單現在使用 Enter 鍵提交，並允許在枚舉欄位中自訂回應
- 支援「命令」欄位作為掛鉤配置中 bash/powershell 的跨平台別名
- 掛鉤配置現在接受超時作為 timeoutSec 的別名
- 修復使用控制鍵對元的處理（包括/terminal-setup中的shift+enter）

## 0.0.423 - 2026-03-06
- 提示使用者輸入具有潛在危險的擴充功能或取代用例的 shell 指令，以及針對惡意攻擊的附加防護措施
- 為 EMU 和 GHE 雲端用戶封鎖/共享要點，並提供清晰的錯誤訊息
- 引出枚舉和布林欄位現在需要 Enter 來確認選擇，✓ 指示符表示確認值，❯ 表示瀏覽遊標
- MCP 伺服器現在可以請求使用者存取 URL 進行帶外交互，例如 OAuth 流或 API 金鑰輸入
- 透過更好的上下文共享來提高探索代理的精度和大型存儲庫支持
- Diff 模式在 Windows 上清晰顯示，並以 CRLF 行結尾

## 0.0.422 - 2026-03-05
- 在驗證和授權錯誤訊息中顯示請求 ID 以協助故障排除
- 除了儲存庫層級的 .github/hooks 之外，還從 ~/.copilot/hooks 載入個人掛鉤
- 時間軸現在在一個方塊中顯示問題，並在自動回覆 Ask_user 時顯示“在自動駕駛儀上進行最佳猜測”
- 增加對 GPT-5.4 模型的支持
- 外掛程式快取自動從損壞或不完整的克隆中恢復，無需手動幹預
- 當未安裝 git 且使用遠端插件或市場時，顯示清晰、可操作的錯誤訊息
- 在替代畫面中複製到剪貼簿後，文字選擇仍然存在
- 在回應流期間或開啟彈出視窗時捲動時，捲動檢視不再跳到較早的訊息
- 新增 copy_on_select 設定選項以在替代畫面模式下自動將所選文字複製到剪貼簿
- IME 候選視窗出現在 CJK 輸入中正確的遊標位置
- 在替代螢幕模式下為 /diff 新增滑鼠滾動支持
- 減少長時間會話中替代螢幕模式下的記憶體使用量
- 當配置 git color.diff=always 時，Diff 模式現在可以正常運作
- 在 Windows 上開啟連結可以正確處理帶有 & 查詢參數的 URL
- @-提及文件完成始終反映工作目錄的當前狀態
- 用於取消的 ESC 鍵在 tmux 和其他非 kitty 終端中可以正常工作
- 按一下提示輸入重新定位文字遊標
- 新增 /copy 指令將最後的回應複製到剪貼簿
- 替代螢幕模式下的連結使用下劃線樣式呈現，以獲得更好的可見性
- /delegate 提示在多遠端儲存庫中輸入目標遠端並澄清確認文本
- GitHub MCP 伺服器在同時具有 Azure DevOps 和 GitHub 遠端的儲存庫中保持啟用狀態
- 內聯程式碼中的冒號在 Markdown 表中正確呈現
- 在幫助對話方塊中按 Ctrl+C 現在可以乾淨地關閉它
- 插件提供的 LSP 伺服器現在已載入、啟動並顯示在 /lsp show 中
- 在所需的枚舉欄位中按 Enter 鍵現在可以選擇反白顯示的選項
- 隱藏嘈雜的待辦事項簿記查詢並在時間軸中顯示依賴項詳細信息
- 在包含大量文件的目錄中工作時，CLI 不再掛起幾分鐘
- 新增 --output-format json 標誌以在提示模式下發出 JSONL 以進行程式設計集成
- 新增 exitPlanMode.request 協定方法以支援 SDK 計劃審批
- 後台 shell 指令和代理完成時自動通知
- GitHub MCP 伺服器連線狀態在狀態指示器中準確追蹤和計數
- 按 Ctrl+R 透過反向增量搜尋來搜尋指令歷史記錄（如 Bash）
- 長差異線不再溢出並包裹在差異視圖中
- 新增啟動提示鉤子以在會話啟動時自動提交提示或斜線命令
- 當遊標位於行尾時，Ctrl+K 連接行，符合標準 Emacs/終端行為
- 跨輸入區塊分割的轉義序列不再洩漏到文字輸入中
- 將 `launch_messages` 配置設定重新命名為 `companyAnnouncements`
- 當終端機交給外部編輯器時顯示等待訊息
- 支援配置中的enabledPlugins，以便在啟動時自動安裝插件
- 改進反向歷史搜尋中的按鍵綁定：Ctrl+J 接受，Ctrl+G 取消
- 將儲存庫配置從 `.github/copilot/config.json` 重新命名為 `settings.json`
- 支援從 ssh:// URL 安裝插件
- 會話使用指標（請求、令牌、程式碼變更）現在在每個會話結束後儲存到 events.jsonl

## 0.0.421 - 2026-03-03
- 自動駕駛權限對話方塊出現在第一次提示提交時，而不是模式切換時
- 自動主題現在讀取您終端的 ANSI 調色板並直接使用它，因此顏色與您的終端主題相匹配
- 使用 MCP Eliitations 為 Ask_user 工具新增結構化表單輸入（實驗性）
- 插件指令從專案級 .claude/settings.json 讀取 extraKnownMarketplaces 以實現 Claude 相容性
- Git hooks 可以透過 COPILOT_CLI=1 環境變數檢測 Copilot CLI 子進程以跳過互動式提示
- 在會話恢復或終端狀態轉換期間，虛假的「寫入 EIO」錯誤條目不再出現在時間軸中
- 基於 Python 的 MCP 伺服器不再因緩衝標準輸出而逾時
- 當 --model 標誌指定不可用的模型時出錯
- MCP 伺服器可用性在登入、切換帳戶或登出後正確更新
- 在狀態列中的分支名稱旁顯示可點選的 PR 參考
- 新增 --plugin-dir 標誌以從本機目錄載入插件
- 滑鼠文字選擇自動複製到 Linux 主選擇緩衝區（中鍵點擊貼上）
- 修正 VS Code 的 shift+enter 和 ctrl+enter 鍵綁定以實現多行輸入
- 使用一致的 ~/.copilot/pkg 路徑進行自動更新，而不是 XDG_STATE_HOME
- ACP 用戶端可以透過會話配置選項來配置推理工作
- 點擊終端機中的連結以在預設瀏覽器中開啟它們
- 透過 .github/copilot/config.json 支援儲存庫級配置，以實現市場和啟動訊息等共享專案設置
- 在替代螢幕模式下運作時，串流輸出不再被截斷
- Windows 上右鍵貼上不再產生亂碼
- Windows 上的 Shell 命令輸出不再在時間軸中呈現為“未檢測到任何更改”
- 使用 # 引用選擇器時，GitHub API 錯誤不再在終端機中顯示為原始 HTTP 訊息
- Markdown 表格以適當的列寬、自動換行和適應終端寬度的 Unicode 邊框進行渲染
- MCP 引出表單顯示更高的多行文字輸入，隱藏單一欄位表單的選項卡欄，並修復欄位導覽上的錯誤閃爍

## 0.0.420 - 2026-02-27
- 自動更新現在也更新二進位可執行文件，而不僅僅是 JS 套件
- 強制推送和基於標籤的安裝後，外掛程式和市場 git 儲存庫可以正確更新
- 502 錯誤閘道錯誤會自動重試，且不再使原始 HTML 輸出的會話崩潰
- 複製提示在 macOS 上的 Ghostty 中顯示 cmd+c，並右鍵單擊作為所有終端的替代方法
- 輸入 # 來引用 GitHub 問題、拉取請求和討論

## 0.0.419 - 2026-02-27
- 新增 /chronicle 指令，其中包含站立、提示和改進由會話歷史記錄支援的子命令（實驗性）
- 向左或向右滾動不再觸發意外的滑鼠按鈕按下
- 新增 Ctrl+F/Ctrl+B 作為在替代畫面檢視中捲動的向下/向上翻頁快捷鍵
- 新增 --mouse/--no-mouse 標誌和滑鼠配置以在替代畫面中停用滑鼠模式
- Home 和 End 鍵跳到替代螢幕滾動緩衝區的頂部和底部
- 新增 Ctrl+G 鍵盤快速鍵，用於在外部編輯器中編輯提示並消除 UI 元素
- /mcp enable 適用於在配置之前自動停用的內建伺服器
- CLI 旋轉器停止，代理完成工作後可以看到最終代理回應
- 自動主題現在使用終端的實際 ANSI 調色板，以便在任何終端主題上獲得更準確的顏色
- 命令、args 或 cwd 欄位中引用的 MCP 伺服器環境變數自動包含在伺服器環境中
- 當尚未啟動會話時 /diagnose 顯示有用的訊息
- MCP 伺服器名稱現在支援點、斜線和 @ 字符，從而啟用 npm 樣式名稱，例如 @modelcontextprotocol/server 和 io.github/server

## 0.0.418 - 2026-02-25
🎉 Copilot CLI 現已[全面可用](https://github.blog/changelog/2026-02-25-github-copilot-cli-is-now-generally-available) 🎉

- 防止特工意外自殺
- 刪除 --disable-parallel-tools-execution 標誌和 parallel_tool_execution 設定選項
- 在plugin.json中指定為檔案路徑的插件代理正確載入

## 0.0.417 - 2026-02-25
- 新增 /research 指令，透過可匯出的報告進行深入研究
- 開啟新會話時，MCP 伺服器不再間歇性載入失敗
- 插件代理和技能安裝後立即可用，無需重啟
- 插件技能和命令從plugin.json中聲明的自訂路徑加載
- Alt+退格鍵正確註冊為退格鍵而不是刪除

## 0.0.416 - 2026-02-24
- 使用描述、範例和排序標誌擴充 `--help` 內容
- 當 Copilot MCP 政策不允許時阻止第三方 MCP 伺服器
- 串流響應大小計數器在工具呼叫和推理期間不斷更新，並在請求之間重置
- 狀態行在窄終端上自動切換為兩行佈局，保持 CWD、分支和模型資訊在任何終端寬度下可讀
- 撤銷操作現在始終需要確認

## 0.0.415 - 2026-02-23
- 使用 UTF-8 BOM（在 Windows 編輯器中常見）保存的技能檔案現在可以正確加載，而不是因 frontmatter 解析錯誤而失敗
- 自訂代理支援 `model` 欄位來指定要使用的模型，未知欄位現在會發出警告而不是阻止代理載入
- 計劃批准選單顯示模型策劃的操作，並首先突出顯示推薦選項，包括用於並行工作的自動駕駛儀+車隊
- 環境載入指示器不再在 MCP 啟動錯誤或會話恢復時無限期掛起
- 新增 show_file 工具，用於向使用者呈現程式碼和差異
- 新增環境載入指示器，顯示正在載入的技能、MCP、插件等
- 具有巨大單行的 MCP 工具結果被正確截斷
- /plugin market add 和 /plugin install 支援包含空格的本機路徑
- `/mcp show` 將伺服器分為使用者、工作區、外掛程式和內建部分，並使所有伺服器均可導航
- 當詢問時，代理商現在知道哪個型號正在為其供電
- Ctrl+A/E 循環顯示換行輸入中的可視線； Home/End 在視線範圍內導航； Ctrl+Home/End 跳到文字邊界

## 0.0.414 - 2026-02-21
- 探索代理現在可以使用 GitHub MCP 工具（如果可用）
- 使用自動駕駛儀接受排程時顯示權限提升對話框，以防止自動拒絕工具錯誤

## 0.0.413 - 2026-02-20
- 修正了 Copilot API URL 不受尊重的問題
- 顯示 gpt 模型推理的標題內容
- LSP請求逾時時間從30秒增加到90秒，減少逾時失敗
- 修正了工具呼叫完成（特別是子代理呼叫）時替代螢幕時間軸條目不更新的問題
- ctrl+insert 現在可用於複製替代螢幕檢視中選取的文本
- 修正 read_bash、write_bash 和 stop_bash 進行中圖示顯示實心點而不是空圓圈
- 使用 `--experimental` 標誌運作時預設啟用替代螢幕模式
- 提高大型倉庫中的代碼搜尋速度
- 會話資訊在替代畫面模式下呈現在主視圖頁腳中
- 使用允許工具的 YAML 數組語法的技能檔案現在可以正確加載
- 支援 market.json 外掛程式條目中的遠端插件來源（GitHub 儲存庫和 git URL）
- 啟動時自動將使用者從 claude-sonnet-4.5 遷移到目前預設模型
- Ctrl+A、Ctrl+E 和 Ctrl+U 導航到邏輯行邊界（換行符號）而不是視覺換行邊界
- 新增可設定狀態行支持，以透過自訂 shell 腳本顯示動態會話訊息

## 0.0.412 - 2026-02-19
- 改進快速幫助可訪問性：螢幕閱讀器友好的選項卡標籤、重新排序的佈局和分組的 `help commands` 輸出
- 從 `/agents` 選擇器中隱藏帶有 `user-invocable: false` 的自訂代理
- 設定檔語法錯誤現在顯示警告，而不是無提示地崩潰
- 使用 Authenticode 對 Windows 本機預先建置進行簽名，以防止本機模組的防毒隔離
- 允許 `/reset-allowed-tools` 在代理執行期間執行
- 具有無效工具模式的 MCP 伺服器不再遺失所有工具
- 替代螢幕模式不再在長時間會話中消耗不斷增加的內存
- 新增 `/mcp reload` 指令以重新載入 MCP 配置
- 技能支援`disable-model-invocation` frontmatter 字段
- /fleet Orchestrator 驗證子代理工作
- 棄用 gpt-5 模型
- Windows 斜線標誌（例如 `xcopy /E /I`）不再被視為檔案路徑
- 當啟動時技能載入失敗時，在時間軸中顯示警告，並提示運行 /skills 了解詳細信息
- 停用橫幅時消除啟動時橫幅字元閃爍
- 使用 ctrl+y 在終端編輯器中編輯計劃
- Windows 現在支援終端編輯器
- 在 lsp.json 中設定 LSP 伺服器請求逾時
- 新增 `/update` 指令來查看變更日誌和更新說明
- 新增具有計劃批准對話框的 exit_plan_mode 工具，用於審查和接受計劃
- 支援 ~/.copilot/instructions/\*.instructions.md 檔案以取得所有儲存庫中的使用者層級指令
- 在替代螢幕文字選擇中新增雙擊單字和三次點擊行選擇
- 使用 ctrl+x ctrl+e 在您喜歡的終端編輯器中編輯提示
- 防止 Windows 終端機出現假錯誤訊息
- 在 AskUser 提示中鍵入 `?` 不再觸發快速幫助覆蓋
- 改進 SQL 工具時間軸條目
- 減少長時間會話期間替代螢幕模式下的記憶體使用量
- /fleet 模式並行調度更多子代理以加快執行速度
- 啟用替代螢幕模式時，說明選擇器將作為全螢幕替代螢幕視圖開啟
- 啟用替代螢幕模式時，技能選擇器將作為全螢幕替代螢幕視圖開啟
- 指令檔不再需要 YAML frontmatter — 普通 Markdown 檔案可使用自動產生的名稱和描述
- 當存在多個會話時，會話選擇器會立即打開，而不會出現載入閃現
- 滑鼠事件座標片段不再出現在輸入欄位中
- 新增跨會話記憶：跨會話詢問過去的工作、文件和 PR（實驗性）
- 將 `--bash-env` 標誌新增至 shell 會話中的來源 BASH_ENV
- 還原 `ctrl+x /` 作為執行指令的備用捷徑，同時保留輸入
- /clear 保留代理模式（自動駕駛、計劃或互動）
- MCP 錯誤訊息包含伺服器名稱
- 拖曳時時間軸中的文字選擇不再溢出到提示區域

## 0.0.411 - 2026-02-17
- 改進策略拒絕存取時的錯誤訊息和指導
- 自訂代理使用 `disable-model-invocation` 而不是 `infer` （向後相容）
- 增加對 Claude Sonnet 4.6 模型的支持
- 記憶體儲存在時間軸中顯示主題、事實和引文
- 製表符補全尊重突出顯示的斜線指令選擇
- 支援來自 Windows 裝置登錄的 MCP 伺服器
- 文字選擇現在可以在替代畫面模式下的頁尾區域中使用
- 支援 `--alt-screen on` 和 `--alt-screen off` 語法
- 新增 `include_coauthor` 設定選項以停用 git 提交中的共同創作預告片
- 用於規劃模式、自動駕駛儀、車隊和工作區文件的 SDK API
- 自動駕駛模式和 /fleet 指令現在可供所有使用者使用
- 將選取範圍拖曳到邊緣時，備用螢幕視窗會自動捲動
- 互動式 shell 指令在所有版本的 Windows 上完成
- 減少長時間會話期間替代螢幕模式下的記憶體使用量
- 在替代螢幕模式下使用 --resume 時，會話選擇器不再閃爍
- 終端鈴聲在代理完成時響起一次，而不是在每個工具完成時響起
- 自訂指令檔案無論大小寫均可識別
- 有語法錯誤的 PowerShell 指令不再掛起
- 提升 --alt-screen 模式下的文字選擇回應能力
- 遊標在暫停時顯示，在恢復時隱藏

## 0.0.410 - 2026-02-14
- 修復了快速記錄導致的高記憶體使用率
- Shell 模式貼上原始文字而不是貼上標記
- 減少編碼流塊的記憶體使用
- 修復替代畫面和時間軸 URL 呈現，以保留長連結而不被截斷
- 透過在壓縮後驅逐瞬態事件來減少長會話中的記憶體成長
- 修正了載入大型會話時記憶體使用率過高的問題
- 修正了具有快速輸出的 shell 命令期間的高記憶體使用率
- 新增 `/init suppress` 來控制每個儲存庫的初始化建議
- 連接到 IDE 時，在狀態列中顯示 IDE 檔案選擇指示器
- 新增儲存庫等級設定以停用單一驗證工具
- ACP伺服器支援載入現有會話
- 在替代螢幕模式下向上翻頁/向下翻頁鍵盤滾動
- 新增 Unix 平台上的 Ctrl+Z 暫停/恢復支援
- MCP 伺服器 cwd 設定中支援波形符號 (~) 擴展
- 支援 ctrl+n 和 ctrl+p 作為箭頭鍵替代
- 在空提示符號下使用 ctrl+d 退出 CLI
- 修復未知選項「--no-warnings」錯誤
- Shift+Enter 使用 kitty 鍵盤協定在終端機中插入換行符
- MCP伺服器清單選擇在刪除後正確調整
- Shell 模式從 Shift+Tab 循環中刪除，只能透過 `!` 存取
- 使用一致的圖示和版式改進/任務對話框
- 從替代畫面退出不再重播完整的會話歷史記錄
- MCP 伺服器錯誤和載入問題出現在時間軸中
- 透過幀合併和更流暢的替代螢幕動畫減少輸入抖動
- 擴展技能名稱驗證以支援底線、點和空格；使名稱和描述在技能前題中可選，並具有合理的後備
- 新增由預告片共同創作的 Copilot 到創建的 git 提交

## 0.0.409 - 2026-02-12
- /diff 在替代螢幕模式下使用全螢幕
- 快速幫助疊加：按 `?` 查看分組的快捷鍵和命令，使用箭頭鍵導航
- 在螢幕閱讀器模式下主題預覽出現在主題清單上方
- 將 `list_copilot_spaces` 工具新增至預設 GitHub MCP 配置
- 子代理回傳完整的回應
- CLI 現在與 VS Code 集成，使用 /ide 獲取更多資訊。
- 具有長差異的權限提示可以在替代螢幕模式下捲動
- 包括預設插件市場（copilot-plugins、awesome-copilot），以便更輕鬆地發現插件

## 0.0.408 - 2026-02-12
- 新增 `/streamer-mode` 以隱藏串流傳輸的預覽模型名稱和配額詳細信息
- 讓 shellId 更靈活，在傳遞數字時不會出錯
- 當分離的外殼被殺死或刪除時，後台任務提示會更新
- 在 --alt-screen 模式下新增滑鼠文字選擇
-！具有大輸出的命令不再使 CLI 崩潰
- 修正在替代螢幕模式下調整終端大小時出現的重複/重影線
- MCP 伺服器尊重 `cwd` 工作目錄屬性
- 新增子字串匹配到斜線指令自動完成
- 將運行命令快捷方式從 ctrl+p 更改為 ctrl+s

## 0.0.407 - 2026-02-11
- 改進提示模式下的身份驗證錯誤訊息
- 配額超出錯誤連結到副駕駛設置，並提供可操作的指導
- 主題選擇器顯示差異和降價的即時預覽，添加色盲和藍色盲主題變體
- 新增 `/on-air` 模式以隱藏串流傳輸的模型名稱和配額詳細信息
- 在 read_agent 時間軸條目中顯示代理類型和描述
- `/tasks` 顯示後台代理的近期活動
- 新增實驗備用螢幕緩衝模式：--alt-screen
- 在 shell 中查詢終端狀態的互動式程序
- 當預設模型被策略阻止時，子代理會回退到會話模型
- 在 session.list SDK 回應中公開會話上下文
- 鍵盤快速鍵提示在整個 CLI 中以粗體樣式顯示一致
- 新增`tools.list` RPC來查詢可用的內建工具
- 當伺服器錯誤中斷時，串流回應會自動重試
- 新增選項以永久批准某個位置的工具權限
- 新增 `/instructions` 指令來檢視和切換自訂指令文件
- Ctrl-b 和 ctrl-f 遊標移動現在可在所有平台上使用
- Ctrl+d 現在支援刪除遊標後的字符，佇列移至 ctrl+q（或 ctrl+enter）
- 編輯 MCP 伺服器顯示現有設定值
- `--resume` 使用提供的 UUID 建立新會話
- 透過 `.vscode/mcp.json` 新增工作區本機 MCP 配置
- `/skills` 指令的技能變更立即生效
- /session 使用字串僅顯示可用的子命令
- 接受提示的斜線命令現在可以在緊跟換行符的情況下工作
- 從狀態列中刪除不需要的字符
- 自動駕駛模式適用於指定顯式工具的自訂代理
- 更新了 node-pty 以修復檔案描述符洩漏
- Windows 斜線標誌（例如 `dir /B`）不再被視為檔案路徑
- 導航檔案時差異模式不再閃爍
- 當伺服器名稱不存在時，/mcp停用和/mcp啟用顯示明確的錯誤
- 使用 Microsoft OAuth 的 MCP 伺服器自動配置，無需手動設定客戶端 ID
- Tab 向前循環模式，Shift+Tab 向後循環； shell 現在是一種模式
- Ctrl+P 執行斜線指令，同時保留輸入（取代 Ctrl+X → /）
- 終端標題適用於所有 TTY 終端，而不僅僅是選擇少數終端
- 預設情況下，CI 環境中會停用說明文字註解自動更新
- 終端機選項卡在空閒時顯示會話標題
- Ask_user 工具一次詢問一個問題，以實現更清晰的交互

## 0.0.406 - 2026-02-07
- 新增對 Claude Opus 4.6 Fast（預覽版）的支持
- 在非互動模式輸出中顯示 Markdown 格式
- 當使用者沒有 Copilot 訂閱時顯示警告
- 來自插件的命令現在轉換為技能
- 新增`/changelog`指令來查看發行說明
- 外掛程式市場添加接受 URL 作為來源
- `--no-experimental` 標誌禁用實驗功能
- CLI 介面渲染時沒有額外的空白行
- `/mcp show` 顯示 MCP 工具的啟用/停用狀態
- MCP 工具回應現在包含結構化內容（圖像、資源），以便在 VS Code 中顯示更豐富的 UI

## 0.0.405 - 2026-02-05
- 外掛程式和市場名稱支援大寫字母
- `/experimental` 顯示列出實驗功能的幫助畫面
- 修復 SQL 工具斷開連線的問題
- 外掛程式可以捆綁 LSP 伺服器配置

## 0.0.404 - 2026-02-05
- 增加對 claude-opus-4.6 模型的支持
- `/allow-all` 和 `/yolo` 立即執行
- MCP 伺服器同時關閉以提高效能
- Cancel --resume 會話選擇器以開始新會話
- 當未指定工具參數時，MCP 伺服器設定預設為所有工具
- 新增`/tasks`指令檢視和管理後台任務
- 為所有使用者啟用後台代理
- 簡化和澄清 `/delegate` 命令訊息傳遞
- GITHUB_TOKEN 環境變數現在可以在代理 shell 會話中存取

## 0.0.403 - 2026-02-04
- Windows 工作管理員顯示正確的應用程式名稱
- 引入安全檢查，防止使用應用程式套件以外的模組
- ACP 模型資訊包括使用乘數和啟用狀態
- 修復邏輯檢查使用者組織成員資格
- 在更新插件之前停止 MCP 伺服器
- 分離的 shell 進程適用於普通 macOS 安裝
- 無論選擇為何，退出鍵總是會中止權限對話框
- 外掛技能在提示模式下運作
- 當 CLI 更新設定檔時，設定檔會保留自訂字段
- 支援模型預設啟用推理摘要
- 支援自訂代理 frontmatter 中的逗號分隔工具
- 具有未知 frontmatter 欄位的技能現在會載入警告，而不是默默地跳過

## 0.0.402 - 2026-02-03
- ACP伺服器支援代理和排程會話模式
- MCP配置適用於ACP模式
- 代理建立精靈樣式改進
- 具有未知欄位的自訂代理會載入警告而不是錯誤
- 自訂代理作為子代理運行時接收環境上下文
- 插件可以為會話生命週期事件提供掛鉤
- 插件更新命令適用於直接插件並處理 Windows 檔案鎖
- 卸載插件時停止 MCP 伺服器

## 0.0.401 - 2026-02-03
- 支援`.agents/skills`目錄自動載入技能
- 改進在模型系列之間切換時對聊天歷史記錄的處理
- 傳回結構化內容的 MCP 工具現在可以在 CLI 中正確顯示
- 支援 Claude 風格的 .mcp.json 格式，無需 mcpServers 包裝器
- 在 VS Code 整合終端機中使用 shift+enter 鍵綁定插入新行
- 大型多線貼上工作正常
- ACP 終端機驗證將正確的參數傳遞給登入
- 箭頭和特殊鍵按下時可靠工作
- 斜線指令幽靈文字正確附加
- 新增 `copilot login` 子命令並支援 ACP 終端驗證
- 新增agentStop和subagentStop掛鉤來控制代理完成
- CLI 優雅地處理未知按鍵
- /diff 使用雙列佈局顯示準確的行號

## 0.0.400 - 2026-01-30
- 新增MCP伺服器指令支援
- 時間軸顯示使用者對 `ask_user` 工具提示的回應以及使用者名稱
- 有序列表顯示為數字而不是破折號
- 使用 `/theme` 指令和 GitHub 深色/淺色主題新增主題選擇器
- 修正了在 Windows 終端機上貼上大內容的支持
- 更好地處理來自 grep 和 glob 工具的大型結果，以避免記憶體問題
- CLI 現在在關閉時發送 DELETE 請求以刪除 MCP 伺服器
- 修正了無法使用箭頭鍵退出選擇清單中的文字輸入的問題
- ACP伺服器支援在會話期間更改模型
- ACP 伺服器支援權限標誌：--yolo、--allow-all 等以及權限配置
- 思考時在終端選項卡中顯示進度指示器
- 刪除捆綁的 LSP 伺服器（TypeScript、Python）
- 提高與使用 OAuth 的遠端 MCP 伺服器的兼容性
- Markdown 表格標題以粗體顯示
- 新增自動駕駛模式以自主完成任務（實驗性）
- 將模糊搜尋加入模型選擇器
- 清單選擇器中的自由格式文字輸入工作正常
- 新增 `copilot plugin` 子指令用於非互動式外掛程式管理
- CLI 在包含許多訊息的會話中回應更快
- Shell 路徑偵測更準確地處理空格、引號和 Windows 開關
- Diff 模式文件清單使用輪播導航，一次最多顯示 5 個文件
- 按住退格鍵繼續刪除文字
- 更好地支援 UNIX 鍵盤綁定（Ctrl+A/E/W/U/K、Alt+箭頭）和各種文字輸入中的多行內容
- 新增 `launch_messages` 配置用於啟動公告
- 程式碼審查工具透過忽略建置工件並限制為 100 個檔案來處理大型變更集

## 0.0.399 - 2026-01-29
- 壓縮訊息顯示更清晰的命令提示以查看檢查點摘要
- 按 Ctrl+X 然後 / 執行斜線指令而不會遺失輸入
- 改進 `/diff` 指令，提供更好的視覺指示器和滾動加速
- 新增 `/allow-all` 和 `/yolo` 命令以在會話期間自動批准所有權限
- 為代理建立精靈新增 Copilot 選項，以根據初始代理描述產生名稱、描述和說明
- 新增用於程式碼智慧的 LSP（語言伺服器協定）工具（需要實驗標誌）
- 會話從第一則訊息中取得人工智慧產生的名稱
- 對話歷史壓縮後技能仍然有效
- /usage 現在包含子代理（例如通用代理）的令牌消耗
- 支援 `.claude/commands/` 單一檔案指令作為技能的更簡單替代方案
- 技能在 Windows 上正確加載
- 新增 `/diff` 指令來查看會話更改
- 使用雙 Esc 鍵撤銷/倒回先前的狀態

## 0.0.398 - 2026-01-28
- 修正了導致代理 shell 呼叫出現「無效會話 ID」錯誤的迴歸
- CLI 標頭對窄終端中的路徑使用中間截斷，保留第一個和最後一個資料夾
- 父目錄中的技能現在可以呼叫並在非 git 目錄中工作

## 0.0.397 - 2026-01-28
- `/mcp show <server-name>` 顯示伺服器詳細資訊和可用工具
- 標題佈局更好地適應狹窄的終端寬度
- 計劃模式輸入文字更具可讀性
- 貼上到提示中超過 30 KB 的內容會自動儲存到工作區文件中
- Homebrew 工具在使用 zsh 作為預設 shell 的 macOS 上正常運作
- 新增 --acp 標誌以作為代理客戶端協定伺服器啟動
-目錄現在出現在 @提及自動完成中
- 會話摘要顯示準確的行數

## 0.0.396 - 2026-01-27
- 技能名稱可以包含大寫字母
- 鍵入時 Ctrl+E 將遊標移到行尾而不擴展時間軸
- `/skills add` 適用於直接包含 SKILL.md 的目錄
- 子代理時間軸條目以粗體大寫名稱顯示
- 時間軸條目顯示成功狀態的實心圓圈
- 改進 UI 元素的水平對齊方式
- 簡化壓縮時間軸條目
- 透過互動式 CLI 精靈建立自訂代理
- 工具過濾標誌現在適用於子代理
- 錯誤訊息一致引用 /login 和 /logout 指令
- 新增 `copilot version` 和 `copilot update` 指令
- preToolUse 掛鉤可以拒絕工具執行並修改參數
- 修復 bash 會話處理中的 PTY 洩漏
- `/plugin install` 支援 GitHub 儲存庫、URL 和本機路徑
- 新增 `/experimental` 指令和 `--experimental` 標誌以選擇實驗性功能
- 新增`/init`指令來產生Copilot指令
- 重新排序模型選擇器清單以更好地組織
- 外掛程式可以提供自訂代理
- 在 WSL 和 devcontainers 上的 VS Code 中開啟計劃文件
- /diff 從子目錄運行時顯示整個儲存庫的更改
- 當目錄路徑尾部有斜線時，/skills add 可以正確計算技能
- 撤銷/倒回顯示受影響文件的準確計數
- GitHub 上的預發布現在顯示詳細的變更日誌註釋

## 0.0.395 - 2026-01-26
- 選擇轉義項目顯示閃爍遊標以指示文字輸入
- `/mcp show` 顯示所有已設定的 MCP 伺服器，包括預設伺服器和來自其他設定的伺服器。
- `/mcp show` 顯示已安裝插件的伺服器
- 倒帶在非 git 儲存庫或沒有提交的儲存庫中顯示清晰的警告
- 當終端失去焦點時光標隱藏
- 換行時格式化文字和連結顯示正確
- 在代理會話中載入本機 shell 配置
- 子代理現在可以使用外掛技能
- CLI 優雅地處理遺失的tree-sitter 檔案而不是崩潰
- 已完成的工具呼叫以提示模式顯示
- 新增註解到 /diff 模式以取得特定於行的回饋

## 0.0.394 - 2026-01-24
- 刪除重複的相同模型指令檔以保存上下文
- 退出摘要顯示準確的使用指標而不是零
- 讓 git 分支在沒有提交的儲存庫中工作
- 在 /delegate 指令中新增對 GitHub Enterprise Cloud (\*.ghe.com) 的支持
- 目錄路徑使用與 git 分支和模型顯示一致的靜音文字顏色
- 插件技能在代理回應中發揮作用
- 時間軸隱藏啟動訊息以減少噪音
- 修正了 read_agent 和其他工具顯示不正確內容的時間軸條目回歸
- Git 狀態按需更新，而不是每 15 秒輪詢一次
- SDK 支援無限會話和自動上下文壓縮
- 記憶體載入錯誤得到妥善處理，無需使用者警告
- `/delegate` 指令接受可選提示，使用對話上下文
- 自動更新不再刪除舊的 CLI 軟體包版本
- 透過更清晰的獨立流程指導來提高任務完成率
- 透過隱藏一些鍵盤提示來簡化底部欄
- 使用 Ctrl+D 將斜線指令與訊息一起排隊
- 按 `/` 搜尋 `/resume` 中的會話

## 0.0.393 - 2026-01-23
- 將對話壓縮狀態顯示為時間軸訊息而不是標題指示器
- 在 Git 儲存庫之外時，記憶體載入不再發出警告
- 新增對 GHE Cloud (\*.ghe.com) 遠端自訂代理的支持
- 插件卸載現在可以正常工作
- 在 tool.execution_start 事件中公開 MCP 伺服器和工具名稱，以便更好地處理錯誤
- 新增 Esc-Esc 來撤銷任何先前快照的檔案更改

## 0.0.392 - 2026-01-22
- 新增 `/plugin` 指令用於插件市場管理
- 新增 /rename 指令作為 /session rename 的別名
- 新增 /plugin update 指令來更新已安裝的插件
- 編輯工具現在在時間軸中展開時顯示差異

## 0.0.390 - 2026-01-22
- 壓縮後保留擴展思維
- 具有 MCP 伺服器的自訂代理可避免不必要的重啟
- 在計劃模式下啟用轉向

## 0.0.389 - 2026-01-22
- 改進 `/session` 指令視覺層次結構與顏色
- 使用不同模型時，子代理會收到正確的工具
- grep 和 glob 工具現在可以找到隱藏檔案和點文件
- 新增適用於 Windows 的 MSI 安裝程式
- 從 npm 套件中刪除 Node 版本要求
- MCP 伺服器現在可以使用具有自動令牌管理和刷新功能的 OAuth 2.0 進行身份驗證
- 在時間軸中顯示 MCP 工具的進度訊息
- 外掛程式可以捆綁安裝時自動載入的 MCP 伺服器
- 使用 /skill-name 等斜線指令呼叫技能
- 新增 `/diff` 命令以查看目前會話期間所做的更改
- 當儲存庫記憶體載入失敗時顯示警告
- 子代理不再掛起使用者輸入請求
- 速率限制錯誤現在在用戶友好的訊息中顯示重試時間
- 在 `/compact` 期間發送的訊息會自動排隊
- 新增 `/models` 作為 `/model` 指令的別名
- 將許可證更改為 MIT 許可證
- 減少歡迎標題中的填充
- Shell 指令 (!) 可以在代理工作時並行執行

## 0.0.388 - 2026-01-20
- 新增`/review`指令來分析程式碼更改
- 讓會話事件訊息更簡潔，視覺上更乾淨
- 在自動更新檢查期間清理舊的軟體包版本以釋放磁碟空間
- `--enable-all-github-mcp-tools` 標誌現在啟用讀寫 GitHub MCP 工具
- `/share gist` 在 GitHub Enterprise Cloud 上顯示具有資料駐留的有用錯誤
- 從 CLI 標頭中刪除提交哈希
- 重新設計 CLI 標題，帶有品牌吉祥物和簡化的歡迎訊息

## 0.0.387 - 2026-01-20
- 技能工具可以處理大型目錄而不超出上下文限制
- 新增ask_user工具用於互動式澄清問題
- 新增計劃模式和專用面板，用於查看實施計劃

## 0.0.386 - 2026-01-19
- 後台壓縮正確保留工具呼叫序列
- 新增`/resume`指令來切換會話

## 0.0.385 - 2026-01-19
- 僅當為使用者啟用記憶體時才包含 store_memory 工具
- 輸入佔位符現在顯示“Type”而不是“Enter”，以避免與 Enter 鍵混淆
- 使用向下箭頭導航歷史記錄時，遊標現在可以正確定位在行尾
- 新的記憶體功能可以優雅地處理 Copilot 在沒有儲存庫的情況下運行
- Control-C 訊息現在顯示 5 秒而不是 1 秒
- 在終端機標籤標題中顯示目前意圖
- 合併所有自訂指令文件，而不是使用基於優先順序的後備
- 透過壓縮檢查點透過自動長時間運行的上下文管理啟用無限會話
- 使用 /agent 在自訂代理之間交換時的 MCP 伺服器管理
- 按 Escape 取消手動 `/compact` 指令
- 從 Codex 到 Opus 的模型切換可以正確保留對話歷史記錄

## 0.0.384 - 2026-01-16
- 新增 `&` 前綴快捷方式，用於委託提示在背景執行（相當於 `/delegate`）
- 製表符完成循環根據鍵入的前綴正確循環，而不是完成的文本
- 允許使用者配置 gpt 模型的推理工作
- MCP 伺服器現在可以為自訂代理正確啟動
- Shell 指令現在在失敗時顯示錯誤輸出
- 修正了某些場景下壓縮後導致模型呼叫失敗的問題
- 登入流程遵循 OAuth Slow_down 間隔並包含偵錯日誌記錄
- 自訂代理發現現在遵循代理定義文件的符號鏈接
- 新增自訂代理委託的額外提示
- 新增 `/cd` 作為 `/cwd` 指令的別名
- CLI 建立的檔案可用於 @-提及
- 為人類克勞德模型啟用擴展思維
- 螢幕閱讀器模式在登入期間顯示靜態文字而不是動畫旋轉器
- 選擇「批准會話」現在會自動批准相同類型的待處理並行權限請求
- 推理視圖設定在會話中持續存在
- 當找不到儲存庫或存取被拒絕時提供更清晰的錯誤訊息
- 在提示中註入存儲庫內存並添加內存存儲工具以記住跨會話的事實
- 當 Copilot 延遲讀取 shell 輸出時顯示延遲時間
- 支援不含方案的代理 URL（例如 localhost:9999）

## 0.0.382 - 2026-01-14
- 增加對 GPT-5.2-Codex 模型的支持
- 新增 `--config-dir` 標誌以覆寫預設配置目錄位置

## 0.0.381 - 2026-01-13
- 新增 --allow-all 和 --yolo 標誌以一次啟用所有權限
- 當輸入斜線指令（如“/q”代表“/quit”）時，幽靈文字和製表符補全顯示正確的別名
- 新增 `/new` 作為 `/clear` 指令的別名
- Shell 模式歷史導覽現在按前綴過濾 - 輸入 `!git` 並按下向上箭頭僅循環瀏覽先前的 git 命令

## 0.0.380 - 2026-01-13
- 檢索模型可以優雅地處理來自防火牆路由的網路錯誤，並適當地引發錯誤
- Bash 指令文字與時間軸事件中的輸出對齊
- 大型輸出提示現在建議針對不同內容類型（包括 JSON）的適當工具
- `--agent` 標誌現在可以在互動模式下工作
- 在拒絕工具權限請求時提供內聯回饋，以便代理不必因拒絕權限而停止
- web-fetch 工具現在拒絕 file:// URL 並建議使用檢視工具
- 終端轉義序列不再顯示為文字輸入
- 自動壓縮在背景運行，不會阻塞對話。
- 中止訊號現在傳播到子代理，允許任務取消以停止所有嵌套代理工作
- 任務工具的自訂代理工具別名
- 使用 view_range 參數時允許讀取 >10MB 的文件
- 具有大量對話歷史記錄的會話在啟動時載入速度更快
- 在副駕駛考慮駕駛或排隊時發送訊息
- 鍵盤快速鍵：Ctrl+O 現在展開最近的時間線，Ctrl+E 展開所有時間軸（Ctrl+R 未綁定以供將來使用）

## 0.0.377 - 2026-01-08
- 大文件訊息現在鼓勵使用 view_range 進行增量閱讀，而不是阻止所有閱讀

## 0.0.376 - 2026-01-08
- 使用 GraphQL ID 或會話選擇器載入遠端會話
- 任務工具子代理現在可以處理映像
- 降級CLI版本不再需要手動清除下載的包
- 大型工具輸出被寫入磁碟，並鼓勵模型使用高效率的搜尋工具

## 0.0.375 - 2026-01-07
- 新增 Ctrl+T 來切換支援模型的推理摘要
- 新增 --share 和 --share-gist 標誌以用於非互動模式下的會話共享
- 批准多個並發編輯時，文件編輯不再掛起
- 有推理的回應不再導致重複的助理訊息
- 子代理執行完成後關閉 MCP 伺服器
- SVG 檔案現在被視為文字檔案而不是二進位圖像
- 修正因聊天完成中使用基於訂閱的路由而導致的「連線錯誤」問題

## 0.0.374 - 2026-01-02
- MCP 伺服器類型說明文字顯示正確的選項
- 當模型不可用時，模型選擇器會顯示具有設定連結的更清晰的訊息
- 新增 95% 令牌限制的自動壓縮和 `/compact` 指令
- 用於探索和管理任務的內建子代理
- 內建 `web_fetch` 工具用於取得網頁內容

## 0.0.373 - 2025-12-30
- 斜線指令中路徑參數的製表符補全，例如 `/cwd` 和 `/add-dir`
- 在 GitHub MCP 伺服器中啟用 Copilot Spaces 工具
- GHE 的 GitHub URL 正確解析
- Kill 命令過濾現在允許當「kill」作為參數出現時執行命令
- 裝置代碼授權輪詢立即開始，而不是等待剪貼簿和瀏覽器

## 0.0.372 - 2025-12-19
- 在選擇或指定停用模型時直接在 CLI 中啟用它們
- 新增 `/context` 指令以視覺化令牌使用情況
- 新增 `--resume` 標誌以在本機繼續遠端會話
- 新增影響存取網路的常見 shell 指令的 URL 權限控制
- 長指令在換行時不再顯示重複的意圖示頭

## 0.0.371 - 2025-12-18
- 普通文字尊重終端的預設前景色
- 更新技能說明文字以引用正確的 ~/.copilot/skills/ 目錄

## 0.0.370 - 2025-12-18
- 使用 --disable-mcp-server 時，現在可以正確忽略已停用的 MCP 伺服器
- 共享會話正確呈現嵌套的降價代碼區塊
- 日誌等級現在輸出該等級和更高嚴重性的所有訊息
- 從系統和環境變數載入CA證書
- 改進 `/model` 錯誤訊息以顯示可用和不可用的模型
- 模型選擇器使用兩列佈局，具有對齊的乘數和更清晰的視覺指示器
- 在 CLI 設定 UI 中新增 STDIO 類型作為 MCP 伺服器本地的同義詞
- 差異顯示使用您配置的 git pager (delta, diff-so-fancy)
- 使用 npm install 中的特定於平台的可執行檔（如果可用）
- 在版本中發佈 CLI 執行檔的 SHA256 校驗和
- 新增 --available-tools 和 --excluded-tools 來過濾模型可以使用哪些工具
- 確保根據橫幅和螢幕閱讀器首選項顯示動畫或非動畫橫幅
- 修正 Codex 模型的截斷邏輯

## 0.0.369 - 2025-12-11
- 增加對 GPT-5.2 的支持

## 0.0.368 - 2025-12-10
- PRU 使用率現在正確顯示
- 修復複選標記和 x 圖示渲染
- 新增 grep 工具 Codex 模型
- 小鍵盤按鍵可在使用 Kitty 鍵盤協定的提示中使用

## 0.0.367 - 2025-12-04
- GPT-5.1-Codex-Max 現已在 GitHub Copilot CLI 中提供

## 0.0.366 - 2025-12-03
- 新增 `infer` 屬性以控制自訂代理工具可見性
- 將 CLI 可執行檔新增至 GitHub 發佈工件
- 為 OpenAI Codex 模型新增 apply_patch 工具鏈

## 0.0.365 - 2025-11-25
- 新增 `--silent` 選項以抑制腳本的統計輸出

## 0.0.364 - 2025-11-25
- 新增差異語法突出顯示
- 修復淺色主題 Markdown 渲染

## 0.0.363 - 2025-11-24
- Opus 4.5、GPT-4.1 和 GPT-5-Mini 現已在 GitHub Copilot CLI 中提供
- 圖像資料貼上現在優先貼上圖像檔案的內容而不是檔案圖示。
- 改進了 shell 工具名稱的時間軸渲染
- 新增對 GITHUB_ASKPASS 環境變數的支援以進行身份驗證
- MCP 伺服器在 `--prompt` 模式下運作

## 0.0.362 - 2025-11-20
- 修復 Windows 上的映像拖放問題
- Shell 指令不再包含在 Bash 和 PowerShell 歷史文件中
- 將剪貼簿中的影像資料直接貼到 CLI 中
- 清理和更新提示以及工具說明更加流暢

## 0.0.360 - 2025-11-18
- 修正檔案操作在等待使用者權限時逾時的問題

## 0.0.359 - 2025-11-17
- 支援透過拖放和貼上圖像檔案路徑將圖像新增至上下文。改進了圖像塊在輸入框中的渲染方式
- 新增 `/share` 指令將會話儲存為 markdown 檔案或 GitHub gist
- 修正了快取令牌在會話結束時顯示為零的錯誤
- 啟用 `USE_BUILTIN_RIPGREP` 環境變數選擇性地使用 PATH 中的 ripgrep
- 修正了從遠端儲存庫的預設分支取得自訂代理導致混淆是否正在使用代理的本機副本的問題
- 修復自訂代理設定問題
- 提高 `Ctrl+C` 效能
- 提高工具參數解析安全性
- 區分工具名稱和路徑並改進工具成功/錯誤圖標
- `copilot -p` 將不再互動式提示權限請求
- 從工具說明中刪除不必要的空格

## 0.0.358 - 2025-11-14
- 恢復版本修復了 GPT-5.1、GPT-5.1-Codex 和 GPT-5.1-Codex-Mini 型號的可用性

## 0.0.357 - 2025-11-13
- 恢復版本修復了圖像大小調整問題

## 0.0.356 - 2025-11-13
- GPT-5.1、GPT-5.1-Codex 和 GPT-5.1-Codex-Mini 現已在 GitHub Copilot CLI 中提供

## 0.0.355 - 2025-11-12
- 使 CLI 代理能夠讀取其自己的 `/help` 和 README，以回答有關其功能的問題
- 改進了對帶有 `.agent.md` 後綴的 VS Code 格式的自訂代理的解析
- 清理工具名稱以修復特殊字元的問題
- 捆綁 `ripgrep` 並新增 `grep` 和 `glob` 工具，以實現更有效率的程式碼庫搜索
- 修正了到達 UI 之前格式錯誤的工具呼叫處理
- 防止 Markdown 訊息中出現雙行換行
- 修正了在多行輸入中使用檔案選擇器導致意外的向上/向下箭頭行為的錯誤
- 修正了自訂代理中的遠端 MCP 伺服器配置未正確取得的錯誤
- 增加了更多細節並改進了 `/session` 命令輸出的樣式
- 從 shell 工具的環境中刪除了內部 `NODE_ENV` 變數
- 修正了使用互動式 shell 工具時的記憶體洩漏問題
- 改進了文件視圖輸出中的行號格式
- 降低了預設的 shell 工具逾時並更新了提示語言，使其不意味著超時意味著失敗
- 確保我們在渲染之前查詢終端背景顏色
- 確保代理不會在自己的 PID 上執行 `pkill`
- 修正了 `copilot` 在中止訊號後不會退出的錯誤
- 確保 Windows 上的 `!` 命令使用 PowerShell（如果可用）
- 修正了 Windows 終端機中不接受鍵盤輸入的錯誤

## 0.0.354 - 2025-11-03
- 當 `-p` 模式因 LLM 後端錯誤（驗證失敗、配額耗盡、網路問題）而失敗時，以非零代碼退出
- 支援MCP伺服器工具通知
- 支援 `COPILOT_GITHUB_TOKEN` 環境變數進行驗證（優先於 `GH_TOKEN`）
- 改進了 shell 指令的安全性，在指令之外提供了更好的定界符處理
- 差異塊線現在可以正確填滿差異框的寬度
- GitHub Actions 環境中的 MCP 伺服器自動使用 `GITHUB_WORKSPACE` 作為工作目錄
- 當不存在本機變更時，`/delegate` 指令現在可以正常運作
- 檔案名稱中帶有特殊字元的自訂代理不再失敗
- 透過 `/model` 命令使用不支援的模型時更好的錯誤訊息
- 替代模型提供者現在在使用不同的 OpenAI 基本 URL 時可以正常運作

## 0.0.353 - 2025-10-28
- 新增了對自訂代理的支援。自訂代理定義從您的儲存庫中的 `~/.copilot/agents`、`.github/agents` 或您組織的 `.github` 儲存庫中提取。您可以使用 `/agent` 斜線指令以互動方式或 `--agent <agent>` 非互動方式明確呼叫代理。代理也作為模型可以在任務完成期間調用的工具提供
- 新增了 `/delegate` 指令，將任務非同步委託給 Copilot 編碼代理。任何未暫存的變更都將提交到新分支，PR 將在您的 GitHub 儲存庫中打開，Copilot 將在後台完成工作。

## 0.0.352 - 2025-10-27
- 改進對包含斜線的 MCP 工具的處理
- 改進使用不受支援的模型時來自 `/model <model>` 命令的錯誤訊息

## 0.0.351 - 2025-10-24
- 改進了我們的路徑檢測啟發式以避免各種煩人的、不必要的權限請求：
    - 執行許多已知為唯讀的標準 bash/PowerShell 指令（修正了 https://github.com/github/sweagentd/issues/7372) 的部分內容）
    - PowerShell 中的 `npm test -- --something` 等指令
    - Shell 重定向，例如您已授予寫入權限的路徑中的 `> some_file.txt`、`> /dev/null` 和 `2>&1`（修正了 https://github.com/github/copilot-cli/issues/211)
    - `gh api` 的參數，如 `gh api /repos/user/repo/ec` （修正 https://github.com/github/copilot-cli/issues/216)
- 改進了 Sonnet 4.5 的提示，以減少工作區中剩餘的中間 Markdown 文件的數量
- 👀 ...[GitHub Universe]見(https://githubuniverse.com/)!

## 0.0.350 - 2025-10-23
- 為了節省上下文視窗空間，我們限制了預設 GitHub MCP 伺服器可用的工具清單。在我們的測試中，模型將使用 [GitHub CLI，`gh`](https://github.com/cli/cli)（如果已安裝）來取代缺少的 MCP 工具。如果您希望開啟所有可用工具，我們新增了 `--enable-all-github-mcp-tools`。
  默认可用工具有： - 代码和仓库导航 - get_file_contents - search_code - search_repositories - list_branches - list_commits - get_commit - 问题管理 - get_issue - list_issues - get_issue_comments - search_issues - PR 管理 - pull_request_read - list_pull_requests - search_pull_requests - 工作流程資訊 - list_workflows - list_workflow_runs - get_workflow_run - get_job_logs - get_workflow_run_logs - 其他搜尋 - user_search
- 將 `sharp` 依賴項捆綁到 CLI 包中 - 我們距離實現 https://github.com/github/copilot-cli/issues/16, 又近了一步，這修復了 Windows 上的一些啟動阻止程序（修復 https://github.com/github/copilot-cli/issues/309 和 https://github.com/github/copilot-cli/issues/287)
- 修正了未正確追蹤輸入令牌的錯誤（修正 https://github.com/github/copilot-cli/issues/337)
- 修正了帶有參數的 MCP 工具在啟用串流時失敗的錯誤
- 新增了額外的偵錯日誌記錄，這將有助於我們調查 https://github.com/github/copilot-cli/issues/346

## 0.0.349 - 2025-10-22
- 該模型現在可以並行調用多個工具。每個工具都必須事先確認。可以使用 `--disable-parallel-tools-execution` 標誌停用此行為
- 新增了 `/quit` 作為 `/exit` 的別名（修正了 https://github.com/github/copilot-cli/issues/357)
- 修正了每個串流輸出區塊作為對話的一部分傳回模型的錯誤（修正了 https://github.com/github/copilot-cli/issues/379)
- 確保在運行路徑權限檢查之前擴展環境變量
- 修正了 Ctrl+K 刪除到輸入框中可視線末端而不是邏輯線的錯誤
- 將臨時目錄新增至模型預設可存取的路徑中（修正了 https://github.com/github/copilot-cli/issues/306)

## 0.0.348 - 2025-10-21
- 副駕駛的輸出現在逐一令牌地傳輸！可以使用 `--stream off` 停用此功能
- 改進了 Copilot CLI 的記憶體佔用，特別是在處理產生非常大輸出的 shell 命令時
- 確保我們在使用 `/terminal-setup` 時保留 VSCode 設定檔中的註解（修正了 https://github.com/github/copilot-cli/issues/325)
- 將 `node-pty` 捆綁到 CLI 套件中－我們距離實作 https://github.com/github/copilot-cli/issues/16 又近了一步
- 修正了本機工具呼叫中斷工作階段的問題（修正 https://github.com/github/copilot-cli/issues/365, https://github.com/github/copilot-cli/issues/364, https://github.com/github/copilot-cli/issues/366)
- 將 LICENSE.md 加入到我們的 Node 套件中（修復 https://github.com/github/copilot-cli/issues/371)
- 新增了對身份驗證狀態變更的偵錯日誌記錄，以深入了解 https://github.com/github/copilot-cli/issues/346

## 0.0.347 - 2025-10-20
- 修正了前端顯示不正確的 PRU 消耗統計資料的更多錯誤
  有關詳細信息，請參閱https://github.com/github/copilot-cli/issues/351#issuecomment-3423735333
- 修正了退格後貼上的輸入內容仍被傳送到模型的錯誤
- 改進了渲染檔案差異時的換行和對齊

## 0.0.346 - 2025-10-19
- 修正了在估計高階請求使用情況時未正確考慮源自設定檔的模型的錯誤
  有關詳細信息，請參閱https://github.com/github/copilot-cli/issues/351#issuecomment-3419045411

## 0.0.345 - 2025-10-18
- 修正了某些使用者的高階請求被多算的錯誤（https://github.com/github/copilot-cli/issues/351). 如果您受到影響，我們正在努力退還您多收的高階請求！

## 0.0.344 - 2025-10-17
- 在提示模式下啟用 GitHub MCP 伺服器
- 新增了對執行分離進程的 bash 工具的支持
- 新增了受支援型號的清單作為 `copilot help config` 文字的一部分
- 修正了會話中止處理，以便在按 <kbd>Esc</kbd> 或強制退出時正確清理孤立的工具調用
- 啟動時強制執行最低節點版本要求
- 簡化了 `/terminal-setup` 的訊息傳遞

## 0.0.343 - 2025-10-16
- ```
  Added new model:
  Run slash model to equip
  Haiku 4.5.
  ```
- 新增了一個標誌來增強 MCP 伺服器配置，以暫時新增或覆寫每個會話的伺服器配置：`--additional-mcp-config`（修正了 https://github.com/github/copilot-cli/issues/288)
    - 您可以透過兩種方式傳遞MCP伺服器設定：
        - 內聯 JSON：`copilot --additional-mcp-config '{"mcpServers": {"my-tool": {...}}}'`
        - 來自檔案（帶有 @ 前綴）：`copilot --additional-mcp-config @/path/to/config.json`
    - 您也可以多次傳遞該標誌（後面的值覆蓋之前的值）：`copilot --additional-mcp-config @base.json --additional-mcp-config @overrides.json`
- 改進了我們的提示，以確保代理在 Windows 上使用 Windows 樣式路徑（修復 https://github.com/github/copilot-cli/issues/261)
- 新增了提示，提示使用者在需要啟用多行輸入時執行 `/terminal-setup`
- 各種視覺改進：
    - 為「思考…」指示器添加了閃爍效果
    - 刪除了時間軸中用戶訊息周圍的框
    - 增加了差異中刪除的行內高光的對比度
    - 允許循環使用斜線指令（從清單底部回到頂部）
    - 對齊許可/確認提示，以確保所有使用相同的視覺風格

## 0.0.342 - 2025-10-15
- 徹底修改了我們的會話日誌格式：
    - 引入了一種新的會話日誌記錄格式，該格式將我們儲存會話的方式與我們在時間軸中顯示會話的方式分開。新的格式更乾淨、更簡潔且可擴展，並且將使我們更輕鬆地實現新功能。
    - 新會話儲存在 `~/.copilot/session-state` 中
    - 舊會話儲存在 `~/.copilot/history-session-state` 中 - 當您從 `copilot --resume` 還原它們時，這些會話將遷移到新的格式和位置
- 預設啟用Kitty協定。現在支援 Kitty 協定的終端機上透過 Shift+Ctrl 支援多行輸入。透過執行 `/terminal-setup` 命令，VSCode 及其分支也支援多行輸入（修復了 https://github.com/github/copilot-cli/issues/14)
- 透過尊重 PAT 和 `gh` 驗證模式的 `GH_HOST` 環境變數來啟用非互動式 GHE 登入（修正 https://github.com/github/copilot-cli/issues/296)
- 透過在 `~/.copilot/config` 中加入持久性 `log_level` 選項，改善了偵錯日誌收集的便利性。可能的值：`["none", "error", "warning", "info", "debug", "all", "default"]`
- 新增了當呼叫 `/model` 導致 Copilot API 錯誤時的偵錯日誌記錄。這應該有助於我們診斷一些策略/模型存取邊緣情況，例如 https://github.com/github/copilot-cli/issues/268 和 https://github.com/github/copilot-cli/issues/116
- 將 `gradlew` 新增至其子指令可以列入白名單的指令清單中（修正 https://github.com/github/copilot-cli/issues/217#issuecomment-3393844685)
- 修正了 MCP 工具呼叫失敗後會話可能進入卡住狀態的錯誤（修正了 https://github.com/github/copilot-cli/issues/312)
- 使 `--help` 文字的輸出更簡潔

## 0.0.341 - 2025-10-14
- 新增了 `/terminal-setup` 命令以在未實現 kitty 協定的終端機上設定多行輸入
- 修正了拒絕 MCP 工具呼叫將拒絕所有未來工具呼叫的錯誤（修正 https://github.com/github/copilot-cli/issues/290)
- 修正了使用參數呼叫 `/model` 無法正常運作的迴歸
- 將每個型號的進階請求乘數新增至 `/model` 清單中（目前，我們所有支援的型號均為 1x）

## 0.0.340 - 2025-10-13
- 刪除了「Windows 支援是實驗性的」警告 - 過去兩週我們在改進 Windows 支援方面取得了一些重大進展！請繼續報告任何問題/反饋
- 透過包含模型呼叫錯誤的 Copilot API 請求 ID 和客戶端錯誤的堆疊追蹤來改進偵錯
- 修正了連續的孤立工具呼叫導致「每個 `tool_use` 區塊在下一則訊息中必須有對應的 `tool_result` 區塊」訊息的問題（修正了 https://github.com/github/copilot-cli/issues/102)
- 新增了在 `-p` 模式下核准新路徑的提示。還新增了 `--allow-all-paths` 參數，用於批准存取所有路徑。
- 變更了 MCP 伺服器設定中環境變數的解析，將 `env` 部分的值視為文字值（修正 https://github.com/github/copilot-cli/issues/26).
  已設定 MCP 伺服器以與 CLI 結合使用的客戶將需要對其 `~/.copilot/mcp-config.json` 進行輕微修改。對於新增了 `env` 部分的任何伺服器，他們需要將 `$` 新增至環境區塊中每個項目的鍵值對的「值」對的開頭，以便將值視為對環境變數的參考。

    例如：之前：

    ```json
    {
        "env": {
            "GITHUB_ACCESS_TOKEN": "GITHUB_TOKEN"
        }
    }
    ```

    在此變更之前，CLI 將從 CLI 環境中讀取 `GITHUB_TOKEN` 的值，並將 MCP 進程中名為 `GITHUB_ACCESS_TOKEN` 的環境變數設為該值。透過此更改，`GITHUB_ACCESS_TOKEN` 現在將設定為文字值 `GITHUB_TOKEN`。要獲得舊的行為，請更改為：

    ```json
    {
        "env": {
            "GITHUB_ACCESS_TOKEN": "${GITHUB_TOKEN}"
        }
    }
    ```

## 0.0.339 - 2025-10-10
- 改進了 `/mcp add` 中 MCP 伺服器的參數輸入 - 先前，使用者必須使用逗號分隔的語法來指定參數。現在，「命令」欄位允許使用者輸入完整的命令來啟動伺服器，就像在 shell 中運行它一樣
- 修正了使用 Kitty 協定時導致包含 `u` 的文字無法正確貼上的錯誤。 Kitty 協定支援仍落後於 `COPILOT_KITTY` 環境變數。 （修復 https://github.com/github/copilot-cli/issues/259)
- 修正了使用 Kitty 協定時導致進程掛在 Windows 上的 VSCode 終端機中的錯誤。 Kitty 協定支援仍落後於 `COPILOT_KITTY` 環境變數。 （修復 https://github.com/github/copilot-cli/issues/257)
- 改進了當沒有可用模型時 `/model` 選擇器中的錯誤處理（修復 https://github.com/github/copilot-cli/issues/229)

## 0.0.338 - 2025-10-09
- 由於觀察到迴歸，將 Kitty 協定支援移至 `COPILOT_KITTY` 環境變數後面 (https://github.com/github/copilot-cli/issues/257, https://github.com/github/copilot-cli/issues/259)
- 修正了帶有空行的多行提示中的換行問題

## 0.0.337 - 2025-10-08
- 新增了對 MCP 伺服器名稱的驗證（修正 https://github.com/github/copilot-cli/issues/110)
- 新增了對 Ctrl+B 和 Ctrl+F 的支持，用於前後移動遊標（修復 https://github.com/github/copilot-cli/issues/214)
- 為支援 [Kitty 協定] 的終端添加了多行輸入支援（https://sw.kovidgoyal.net/kitty/keyboard-protocol/)（部分修復了 https://github.com/github/copilot-cli/issues/14 —— 即將推出更廣泛的終端支援！）
- 更新了 OAuth 登入 UI，以便在產生裝置程式碼後立即開始輪詢（這將更可靠地修復 SSH 邊緣情況，如 https://github.com/github/copilot-cli/issues/89) 中所述）

## 0.0.336 - 2025-10-07
- 透過 HTTPS_PROXY/HTTP_PROXY 環境變數啟用代理支持，無論節點版本為何（修正 https://github.com/github/copilot-cli/issues/41)
- 顯著減少令牌消耗、每個問題的往返次數以及得出結果的時間。我們將在周五的每週更新日誌中分享更多具體數據！
- 透過不依賴 shell 來取得目前工作目錄，提高了檔案寫入效能（尤其是在 Windows 上）
- 修正了 `/clear` 未正確重設上下文截斷追蹤狀態的錯誤
- 隱藏了有關會話恢復的「歡迎使用 GitHub Copilot CLI」歡迎訊息和 `/clear`，以獲得更清晰的外觀
- 改進了存在捲軸的表格的對齊方式
- 改進了 `--help` 的輸出，使其更加簡潔
- 新增了提示，提示使用 `--screen-reader` 啟動的使用者永久儲存此首選項
- 可能改善某些情況下的閃爍問題；我們仍在努力！

## 0.0.335 - 2025-10-06
- 預設在時間軸中顯示檔案差異，從而提高檔案編輯的可見性，無需按 Ctrl+R
- 透過在輸入框中顯示參數提示來改善斜線指令輸入
- 改進了寬度小於80列的視窗中介面的顯示
- 減少了顏色數量並改善了 Markdown 渲染的間距
- 新增了在無法運作的環境中嘗試使用代理支援時的警告（節點<24，未設定所需的環境變數）（https://github.com/github/copilot-cli/issues/41 的更永久修復將於明天推出）
- 將上下文截斷訊息的顏色從錯誤顏色更新為警告顏色
- 修正了在 Windows 上可能無法正確建立 `copilot` 日誌的錯誤
- 修正了具有自訂設定檔的 Powershell 使用者可能在執行命令時遇到問題的錯誤（修正 https://github.com/github/copilot-cli/issues/196)
- 修正了貼上和其他邊緣狀況後提示被截斷的錯誤（修正 https://github.com/github/copilot-cli/issues/208, https://github.com/github/copilot-cli/issues/218)
- 修正了使用者在啟動時儘管已登入但仍會看到登入提示的錯誤（修正了 https://github.com/github/copilot-cli/issues/202)
- 修正了某些環境中的某些 SSH 使用者無法取得 OAuth 登入連結並在嘗試開啟瀏覽器時進程掛起的錯誤（修正了 https://github.com/github/copilot-cli/issues/89)

## 0.0.334 - 2025-10-03
- 改進了貼上大內容的體驗：貼上超過 10 行時，會顯示為 `[Paste #1 - 15 lines]` 等緊湊令牌，而不是淹沒終端。
- 新增了當對話情境接近模型剩餘限制 ≤20% 時將很快發生截斷的警告。此時，我們建議您開始新的會話（改進 https://github.com/github/copilot-cli/issues/29)
- 從持久會話歷史記錄中刪除了退出時的使用統計信息
- 將目前版本新增至啟動日誌以協助錯誤報告
- 如果存在參數，則刪除循環 TAB 自動完成專案。這可以防止執行 `/cwd /path/to/whatever`，點選 `TAB`，然後看到 `/clear` 自動完成

## 0.0.333 - 2025-10-02
- 新增了影像支援！ `@`-提及檔案以將其新增為模型的輸入。
- 改進了對 Node.JS v24+ 使用者的代理支援。請參閱[此評論](https://github.com/github/copilot-cli/issues/41#issuecomment-3362444262) 以了解更多詳細資訊（修正 https://github.com/github/copilot-cli/issues/41)
- 新增了對直接執行 shell 命令並透過在輸入前面新增 `!` 來繞過模型的支援（修正了 https://github.com/github/copilot-cli/issues/186, https://github.com/github/copilot-cli/issues/12)
- 新增了 `/usage` 斜線命令，以提供有關進階請求使用情況、會話時間、程式碼變更和每個模型令牌使用情況的統計資料。此資訊也會在會話結束時列印（修正 https://github.com/github/copilot-cli/issues/27, https://github.com/github/copilot-cli/issues/121)
- 透過以資訊標籤取代時間軸中的圖示來改善 `--screen-reader` 模式
- 新增了 `--continue` 標誌以恢復最近關閉的會話
- 更新了 `/clear` 命令以正確清除舊的時間軸條目/會話資訊（修正 https://github.com/github/copilot-cli/issues/170)

## 0.0.332 - 2025-10-01
- 根據 [GitHub 文件](https://docs.github.com/en/copilot/how-tos/administer-copilot/manage-for-enterprise/manage-access/manage-network-access)) 切換為使用每訂閱 Copilot API 端點（修正 https://github.com/github/copilot-cli/issues/76)
- 修正了 `/user [list | show | switch]` 不包含從所有驗證模式登入的使用者的錯誤（修正了 https://github.com/github/copilot-cli/issues/58)
- 修正了使用 `/user switch` 切換到另一個使用者在 GitHub MCP 伺服器中未生效的錯誤
- 透過停用 `@` 檔案選擇器、`--resume` 會話選擇器和 `/` 命令選擇器中的捲軸，改進了螢幕閱讀器體驗
- 改進了滾動條容器的拋光（增加了寬度，降低了裝訂線的不透明度）
- 對輸入區域進行了細微的視覺改進（將當前模型指示器移至右側，使其不會受到 CWD 的限制，改進了檔案選擇器「索引」指示器的位置，改進了完成功能表中的提示格式）
- 透過排除標題中的 `#` 前綴，提高了 Markdown 的易讀性
- 改進了從 shell 命令中提取路徑以進行權限處理的方式（可能修復 https://github.com/github/copilot-cli/issues/159, https://github.com/github/copilot-cli/issues/67)

## 0.0.331 - 2025-10-01
- 提高了檔案讀取/編輯時間軸事件的資訊密度
- 修正了 `--banner` 幫助文字中的不準確之處；它之前暗示它將持續更改配置以始終顯示啟動橫幅
- 改進了 `/model`s 列表，以確保用戶只能看到他們有權使用的模型 - 以前，如果用戶嘗試使用他們無權訪問的模型（由於其 Copilot 計劃、地理區域等），他們會收到 `model_not_supported` 錯誤。這應該可以防止這種情況發生，甚至不將此類模型顯示為清單中的選項（修正 https://github.com/github/copilot-cli/issues/112, https://github.com/github/copilot-cli/issues/85, https://github.com/github/copilot-cli/issues/40)
- 修正了在多行提示中按下向下箭頭會環繞到第一行的錯誤（這是實作 https://github.com/github/copilot-cli/issues/14) 的方法）
- 在 `@` 檔案中新增了一個捲軸，提及選擇器，並將活動緩衝區的大小增加到 10 個項目
- 改進了代理執行時間編寫提示的體驗 - 向上/向下箭頭現在可以在 `@` 和 `/` 選單中的選項之間正確導航

## 0.0.330 - 2025-09-29
- 由於 Sonnet 4.5 尚未向所有使用者推出，因此預設模型變更回 Sonnet 4。 Sonnet 4.5 仍可透過 `/model` 斜線指令取得

## 0.0.329 - 2025-09-29
- 新增了對 [Claude Sonnet 4.5](https://github.blog/changelog/2025-09-29-anthropic-claude-sonnet-4-5-is-in-public-preview-for-github-copilot/) 的支持，並將其設為預設模型
- 新增了 `/model` 斜線命令以輕鬆更改模型（修復 https://github.com/github/copilot-cli/issues/10)
    - `/model` 將會開啟一個選擇器來改變模型
    - `/model <model>` 將模型設定為提供的參數
- 在輸入文字方塊上方新增了目前選定模型的顯示（解決 https://github.com/github/copilot-cli/issues/120, https://github.com/github/copilot-cli/issues/108, 中的回饋）
- 改進了使用者提供不正確的命令列參數時的錯誤訊息。 （解決來自 https://github.com/github/copilot-cli/issues/96) 的非互動模式可發現性回饋
- 更改了 `Ctrl+r` 的行為以僅擴展最近的時間軸項目。執行`Ctrl+r`後，可以使用`Ctrl+e`展開所有
- 改進了單字運動邏輯，以更好地檢測換行符：使用單字運動鍵現在將正確移動到一行上的第一個單字
- 改進了輸入框中多行輸入的處理：輸入文字方塊可捲動，僅限 10 行。長提示將不再佔據整個螢幕！ （這是在實作 https://github.com/github/copilot-cli/issues/14) 的過程中
- 刪除了輸入框的左右邊框。這使得從中複製文字變得更容易！
- 新增了與 shell 規則的全域匹配。使用 `--allow-tool` 和 `--deny-tool` 時，您現在可以指定 `shell(npm run test:*)` 之類的內容來匹配以 `npm run test` 開頭的任何 shell 命令
- 改進了 `copilot --resume` 介面，具有相對時間顯示、會話訊息計數，（修復 https://github.com/github/copilot-cli/issues/97)

## 0.0.328 - 2025-09-26
- 改進了 Copilot CLI 被組織策略阻止時收到的錯誤訊息（修正 https://github.com/github/copilot-cli/issues/18 ）
- 改進了使用缺少「副駕駛請求」權限的 PAT 時收到的錯誤訊息（修正 https://github.com/github/copilot-cli/issues/46 ）
- 改進了 `/user list` 的輸出，使其更清楚哪個是當前用戶
- 改進了 `ForEach-Object` 的 PowerShell 解析和命令名稱表達式的檢測（例如 `& $someCommand`）